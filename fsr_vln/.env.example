# Usage: Copy this file to .env and fill in the required values.
# VLM / LLM provider configuration

PROVIDER=vllm
MODEL_NAME = "Qwen/Qwen3-VL-4B-Instruct"
HTTP_ENDPOINT = "http://localhost:8000/v1"
API_KEY = "xxxx" # If your provider requires an API key, set it here. Otherwise, leave it as is or remove it.
